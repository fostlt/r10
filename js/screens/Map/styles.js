import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    mapContainer: {
        marginRight: "auto",
        marginLeft: "auto",
        marginTop: "75%",
        fontSize: 35
    }
});

export default styles;
